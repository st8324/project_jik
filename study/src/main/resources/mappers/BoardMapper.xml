<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="kr.green.study.dao.BoardDAO">
	<select id="selectBoardList" resultType="kr.green.study.vo.BoardVO">
		select * from board 
			order by oriNo desc, groupOrd asc
	</select>
	<select id="selectBoard" resultType="kr.green.study.vo.BoardVO">
		select * from board
			where num = #{num}
	</select>
	<insert id="insertBoard">
		insert into 
			board(title, contents, writer, oriNo, groupOrd)
			select #{board.title}, #{board.contents}, #{board.writer}, 
			<if test="board.oriNo != 0">
				#{board.oriNo}, max(oriNo)+1 from board where oriNo = #{board.oriNo}
			</if>
			<if test="board.oriNo == 0">
				max(num)+1, 0 from board
			</if>
	</insert>
</mapper>
